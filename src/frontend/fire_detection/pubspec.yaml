name: fire_detection
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.2.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  http: ^0.13.3

flutter:
  uses-material-design: true

  # Add the following section to specify assets
  assets:
    - assets/
    - assets/icons/
    - assets/images/landing_image.jpg
